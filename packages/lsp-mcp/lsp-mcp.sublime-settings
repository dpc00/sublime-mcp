{
    // Port for the MCP SSE endpoint (point your MCP client's "url" here, e.g. http://127.0.0.1:9506/sse)
    "mcp_port": 9506,

    // Port for the internal HTTP bridge used by the MCP SSE dispatcher above
    "http_port": 9516
}
