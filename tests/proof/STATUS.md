# Proof status

Scoreboard for `docs/proof-harness-proposal-2026-08-17.md`. A finding only
counts as real once a deterministic test proves it against the actual code —
not because a static review said it "could" happen.

| ID | Status | Test | Evidence |
| --- | --- | --- | --- |
| F1 | PROVED, deferred pending live Claude Code auth probe | `test_f01_claude_legacy_auth.py` | Empty-header client completes a full legacy `/sse` → `/messages` `tools/call` round trip. Not fixed: the one real client this path serves (Claude Code) is documented as sending no auth headers on SSE connect; locking it down without live-testing against that client risks breaking it. This is compatibility debt, not a closed non-issue — when someone can live-test, match whatever Claude Code's client actually sends (even if that is still nothing) rather than inventing a bearer requirement it can't meet. If a change is ever needed without live client access, prefer opt-in strict mode / a separate bind / explicit "intentionally open" documentation over silently locking it down. |
| F3 | PROVED, FIXED | `test_f03_claude_open_diff_timeout.py` | `_claude_open_diff` used bare `event.wait()`. Now `event.wait(timeout=_CLAUDE_DIFF_WAIT_TIMEOUT_SECONDS)` (30 min). Behavioral twin proves a short timeout unblocks with default `DIFF_REJECTED` and does not clear the review registry; accept still returns `FILE_SAVED`. Source lock forbids bare `wait()`. |
| F4 | PROVED, FIXED | `test_f04_notification_crosstalk.py` | Shared hub + unconditional `ide/diffRejected` on close bled Claude lifecycle into Gemini/Qwen subscribers. `close_tab` already erased the marker before close; extended fix: Accept/Reject/`on_close` skip hub notify when the review is Claude-owned (`claude_event`). Gemini-owned closes still publish reject. Behavioral tests drive a real companion SSE subscriber. |
| F6 | PROVED, FIXED (correction moved to the source of truth) | `test_f06_batch_docstring.py` | `python-proxy` batch docstring claimed "one main-thread dispatch"; backend `sublime_mcp._batch` explicitly does not do that. Originally corrected in that one docstring. Superseded 2026-08-19 by the F10 generator work: python-proxy no longer has a hand-written batch docstring, so the correction was moved into the backend `_MCP_TOOLS` entry and now reaches every surface at once (backend MCP, node-proxy dynamic discovery, node-proxy fallback, python-proxy). Tests assert the source of truth, that both generated catalogs carry it, that the two surfaces agree, and that no hand-written `def batch(` reappears in the proxy. |
| F7 | PROVED, FIXED | `test_f07_selection_cap.py` | Context snapshot capped `selectedText` at 16 KiB; `selection_changed` sent full `view.substr(region)`. Both paths now share `build_selection_changed_params` / `truncate_utf8` at `MAX_SELECTED_TEXT_BYTES`. |
| F8 | PROVED, accepted over-claim (not fixed) | `test_f08_is_trusted_hardcoded.py` | `is_trusted=True` is hardcoded at the only production call site; no code path can produce `false`. Not "fixed" because Sublime Text has no real workspace-trust concept to report — inventing a fake untrusted state would be worse than the over-claim. The residual issue isn't "should sometimes be false," it's that the protocol field over-claims a concept ST doesn't have: `isTrusted` is always `true` and clients must not treat it as a real security boundary from this side. If a client ever requires the field, keep it always-true and documented; only consider omitting it if a client is shown to treat "missing" differently from "true" — never flip it to false. |
| F13 | PROVED, FIXED (keep-alive residual also fixed) | `test_f13_request_body_size.py` | Companion HTTP handlers read `Content-Length` bytes with no ceiling. Added `MAX_REQUEST_BODY_BYTES` (64 MiB); requests over the cap get `413`. First fix returned 413 without draining the declared body, desyncing HTTP/1.1 keep-alive on the next request — caught by a flaky test, independently confirmed by Junie's deeper pass. Fixed by draining the declared body before responding (`_reject_body_too_large`) instead of racing a connection close. `test_f13_connection_does_not_desync_after_413` locks this in. |
| F5 | PROVED, FIXED | `test_f05_proxy_timeout.py` | Both proxies applied a hard 10s HTTP timeout. FakeBridge slept 12s then 200; python-proxy raised `httpx.ReadTimeout` while the backend still completed. Fixed with per-endpoint timeouts: `/batch`, `/install_package`, `/search_packages`, `/find_in_files`, `/eval_python_latest`, `/run_build` get 120s; quick reads stay at 10s. Node HTTP client extracted to `packages/node-proxy/http.js` so the same rule is testable without starting MCP stdio. |
| F2 | Out of scope | — | Excluded per repo owner: the `0.0.0.0` bind is an intentional WSL-reachability choice, not a defect. Not tracked here. |
| F9 | NARROWED, FIXED (threads + close); invalid JSON accepted as skip-and-continue | `test_f09_acp_lifecycle.py` | Flood of 40 blocking agent requests started 40 handler threads (proved). `close()` left the reader thread alive (proved). Fixed with a daemon pool of `MAX_AGENT_REQUEST_THREADS` (8) and `close()` now closes the reader and joins the read loop. Invalid JSON is still skipped; a later valid request is answered over both in-memory queues and a real stdio fake peer. Not changed to fail the session — that would be worse than skip-and-continue. `invalid_json_count` records drops. EOF still unblocks pending requests. |
| F10 | PROVED, FIXED (generator shipped, both proxies) | `test_f10_proxy_catalog_drift.py`, `test_f10_python_proxy_catalog.py`, `packages/node-proxy/test_fallback_catalog.mjs`, `tests/test_python_proxy.py` | Real catalog diff proved the drift on both proxies. node-proxy: backend `_MCP_TOOLS` had 220 tools, its hand-maintained fallback had 71, so a `/mcp_tools` discovery miss silently dropped 149 including `batch`. python-proxy: same dual-maintenance defect in a worse form — it has no dynamic discovery at all, so its 72 hand-written tools were the *entire* surface and 148 backend tools were permanently unreachable, not merely dropped on a miss. Fixed by killing both second sources of truth: `tools/generate_fallback_catalog.py` imports the backend headlessly (stubbed `sublime`/`sublime_plugin`), reuses its own `_get_mcp_tools`, and emits `packages/node-proxy/fallback-tools.json` plus `packages/python-proxy/tool_catalog.py` (a module, not data, because that package ships `py-modules` only). node-proxy registers through its existing JSON-Schema→Zod path; python-proxy builds typed FastMCP tools by synthesizing signatures, carrying descriptions and nested schema keywords through pydantic `Field`. Both post to the `POST /{name}` alias every backend tool already has. Locks: shared `--check` fails when either committed catalog is stale; each catalog must equal the backend exactly; neither proxy may contain a hand-written tool list; python-proxy overrides must be declared in `_OVERRIDDEN` (so they replace rather than collide with their generated twin), must name real backend tools, and `tool_catalog` must stay in `py-modules`. Verified by injecting a canary tool into `_MCP_TOOLS`: all four staleness/coverage locks failed, regenerating made them pass, then reverted. Integration-verified end to end: node-proxy against a fake backend that 404s `/mcp_tools` exposed all 220 tools over real MCP stdio with `batch` arguments and typed schemas intact; python-proxy went 72 → 220 with exact backend parity over live MCP stdio; a fake-bridge test records the exact request bodies, proving declared defaults are sent and optional params with no default are omitted rather than sent as `null`. Also fixed a latent bug in the node test's own `_MCP_TOOLS` parser — it read `batch`'s nested per-call `required` instead of the top-level one, unnoticed while `batch` was absent from the fallback. |
| F11 | PROVED code fact, FIXED, live-runtime verified | `test_f11_diagnostics_private_lsp.py` | `_claude_get_diagnostics` looks up `LSP.plugin.core.registry` via `sys.modules` and reads `storage._diagnostics`. Missing registry already returned `[]`. No in-repo public LSP diagnostics iterator; Sublime LSP's documented surface is commands/panels, and `lsp-mcp` uses the same private bag. Wrapped the walk in try/except that prints `LSP diagnostics internals unavailable` and returns `[]` — success path when the private structure is present is unchanged. Live-verified 2026-08-17: called the live function against a real Sublime + Pyright session, got 48 real diagnostics back through the guarded path. Failure branch (an actual LSP internals break) remains proved at source level only — not worth deliberately breaking a working LSP install to exercise it live. |
| F12 | SKIPPED | — | Docs/repo hygiene only, not a mock-server proof target. No concrete stale pointer or duplicated instruction worth changing in this pass; proposal layout names are a historical plan, not the scoreboard. |

Run: `python -m pytest tests/proof/ -v`

Both proxy tool catalogs are generated. After changing `_MCP_TOOLS`, run:

```
python tools/generate_fallback_catalog.py
```

That writes `packages/node-proxy/fallback-tools.json` and
`packages/python-proxy/tool_catalog.py`.
`tests/proof/test_f10_proxy_catalog_drift.py` and
`tests/proof/test_f10_python_proxy_catalog.py` fail if you forget. The
matching MCP-stdio integration checks are
`node packages/node-proxy/test_fallback_catalog.mjs` (no Sublime needed)
and `pytest tests/test_python_proxy.py` (needs a running Sublime).
