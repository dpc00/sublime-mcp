# Session baton - package audit campaign (saved 2026-09-26, Donald stopped for the day)

Read this first, then `.package_skill_test_log.md` (append-only log of every package) and the memory index
(`C:\Users\donal\.claude\projects\C--Users-donal-projects-sublime-mcp\memory\MEMORY.md`).

## Where we are
- 2026-09-26 (later): sublime-mcp discover_tools tree + guide loop finished and committed locally (3eb62b8, not pushed); see the last entry of .package_skill_test_log.md. Audit still paused at #582 haoide (xjsender/haoide already has our issues #219-#221 filed 2026-09-25 - re-check before filing anything).
- Queue file: `C:\Users\donal\AppData\Local\Temp\claude\C--Users-donal-projects-sublime-mcp\165e315a-efcb-49b3-8b31-7320fbdb0c43\scratchpad\campaign\queue.json`
  (3,828 entries; `q[i-1]` = 1-based index i; the older log numbering is off by about one).
- Done through #581 CommandsBrowser (filed Sublime-Instincts/CommandsBrowser #13, #14). **Next: #582 haoide**, then ...
- Method (Donald's rules): INSTALL packages to test them, clone only if absolutely no other option (2026-09-26); ONE package at a time, narrate what/why before any skip; never skip for a missing free prerequisite (install it); only cost is a barrier. Check existing issues FIRST in a separate step (`gh issue list -R owner/repo --state all`), one issue per finding, verify before filing, issue bodies with the Write tool (Bash eats backslashes; re-read backslash lines after posting), repros must be user-reachable, never patch authors' code, no emails to authors, helpful comments on existing issues are fine ("yes in general to good deeds"). Log every package in `.package_skill_test_log.md`.

## Test rig
- Portable ST 4215 at `D:\st_portable_4215`, kept running, driven by sublime-mcp (MCP `sublime-mcp-portable` or HTTP `POST http://127.0.0.1:9510/eval_python`; helper `C:\Users\donal\AppData\Local\Temp\pb.py file.py`). Runs ALL packages on Python 3.14 (not the 3.3/3.8 hosts of the real ST 4200). 5 s main-thread limit; never wait/sleep on the main thread.
- It now has Package Control (copied from the real ST). Install a package with `C:\Users\donal\AppData\Local\Temp\pb_install.py` (edit the name tuple + add the name to `D:\st_portable_4215\Data\Packages\User\Package Control.sublime-settings` `installed_packages`, otherwise PC removes it as an orphan on restart), then run `satisfy_libraries` and read the console. Remove a package by deleting its `.sublime-package` and its settings entry.
- Portable also has LSP + LSP-file-watcher-chokidar + FileWatcher listed as installed (left in place for later LSP packages).
- **Before killing/restarting the portable or removing a package**: check `window.active_panel()` and `computer-use list_apps` for dialogs/input boxes, CLOSE the views that use the package first (removing a package under an open view loops "Error loading syntax/colour scheme" dialogs; a deleted file raises "Save Deleted File?"), then delete `D:\st_portable_4215\Data\Local\*.sublime_session` before a restart so old views don't come back. Restart with the refreshed user PATH (PowerShell: `$env:Path = [Environment]::GetEnvironmentVariable('Path','User') + ';' + [Environment]::GetEnvironmentVariable('Path','Machine')`) so gdb/php/etc. are found.
- Dismiss native ST dialogs with computer-use (`list_apps`, `get_window_state`, click). Never touch the real ST beyond the agreed GlassIt settings/exe. Real key presses WORK (verified 2026-09-26 11:50): the portable is not minimized, and computer-use lists it under the app path `C:\Users\donal\AppData\Local\Temp\ghostshell-sublime-4107-cache\sublime_text.exe` (take the window id from list_apps; it was 1771126), NOT under D:\st_portable_4215. Use press_key Return to choose a quick-panel item (a background click on the quick panel did nothing). If a call says "user input was detected", call get_window_state and retry once. The older note that key presses cannot be delivered was stale. on_text_command hooks still don't fire for bridge-run commands.
- Big console dumps: use Grep on the saved tool-result file, not the whole `get_console_log`.

## Filed this stretch (all logged)
PyRock #20-#29 (abhishek72850/pyrock); FileWatcher #4, #5 (kapitanluffy/sublime-file-watcher); Direnv #7 (misuzu/direnv-subl); OrgExtended #97, #98 (ihdavids/orgextended); StGitlab #9 (tosher/StGitlab); Whiley #5 (Whiley/WhileySyntaxBundle); ReaSyntax #8 (Breeder/ReaSyntax). Comments on existing issues: SublimeGDB #108, Haskell HSX #2. SublimeGDB, FullwidthTabSpaces: tested, no new bug.

## Still held / pending (ask Donald before doing)
- Simplenote: SKIPPED by Donald (he plans Notr package training instead; wait for him).
- AskCodi, AskPerplexity: need API keys/cost - ask. AutoMatlab: paid MATLAB - barrier. Explain PostgreSQL: Donald declined Postgres. StGitlab feature tests need a GitLab account/token (not his).
- Free-prereq packages still to run in the portable now that tools exist: Pretty EDN (babashka), csharpier package, Taskfile, PHP CS Fixer, Rome Formatter, Fantom, Coconut, Acton/isort, Verible, C++ build systems, Inkscape for SVG Viewer, Ruby/RSpec packages, TestRSpec, JSCustom.
- Archived-repo findings (CP Parser, Search in Project, Emoji Code): review with Donald whether to flag on the Package Control channel; no emails.
- Optional retest of Package Control fixes (#1763 junctions, #1771 via #1791) against a disposable junction only, when a new PC build is served.
- Package Control issues: deathaxe answered #1773 ("designed behavior of Python percent formats") and closed #1762 as not planned ("expected for 3rd-party repos") - nothing to do; #1772 and SublimeSnippetMaker#25 closed as completed.

## Machine health snapshot (2026-09-26, Donald asked)
- C: 475.6 GB, 251 GB used, 224 GB free; D: 28.9 GB, 27.7 GB free. NVMe (KIOXIA KBG50ZNV512G) reports Healthy; SMART wear counters need admin (not readable from here).
- Biggest user folders: AppData 85 GB (Local\wsl 17.6, npm-cache 12.8, Programs 8.4, jcode 6.5, Temp 5.4, Sublime Text 3.9), .ollama 36 GB, .codex 6, .local 5.5, Downloads 4.9, .bun 4.2, src 3.6, tools 2.4. `~/data/logs` is only 0.28 GB (363 files).
- Added by this campaign (approx 25-30 GB): npm-cache (part), Temp (5.4 GB: my `q_*` clones only 0.27 GB, `claude` scratch 4 GB), tools (verible, fantom 2.4), winget packages (~2), .dotnet 0.9, pip cache 0.4, Inkscape, PHP, Ruby, WinLibs, Cursor, Discord, GitHub Desktop, direnv, babashka, Task. Nothing was deleted; safe cleanup candidates need Donald's OK (npm cache clean, Temp q_*/scratch, pip cache).

## Cleanup done 2026-09-26 (Donald approved "safe cleanup")
`npm cache clean --force`, `pip cache purge` (403 MB), removed my 162 `q_*` clones in %TEMP%: C: free went 223.6 -> 236.4 GB (12.8 GB freed). Deliberately kept: %TEMP%\claude (queue.json, scratchpads, campaign tools), the `pb*.py`/`chk_*.py` helper scripts in %TEMP%, `%USERPROFILE%\tools`, winget/dotnet installs (needed by held packages). Repo clones for reading are now re-created on demand (`git clone --depth 1 ... %TEMP%\q_<name>`).

## Bare portable instance for sublime-mcp testing (2026-09-26)
`D:\st_bare_4215` = program files only + empty Data + only MCP Commander (ports 9520 HTTP / 9522 MCP). Start with D:\st_bare_4215\sublime_text.exe; drive through HTTP POST http://127.0.0.1:9520/<route> (helper: %TEMP%are_call.py <route> [json], and %TEMP%\pb_bare.py file.py for eval_python). To test a new sublime_mcp.py: copy it over D:\st_bare_4215\Data\Packages\MCP Commander\sublime_mcp.py, kill the bare process, relaunch. In Git Bash never pass a route with a leading slash as a CLI arg (turned into C:/Program Files/Git/...): the helper takes routes without the slash.
